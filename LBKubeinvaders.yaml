apiVersion: v1
kind: Service
metadata:
  name: kubeinvaders-service
spec:
  selector:
    app: kubeinvaders
  ports:
    - port: 80
      targetPort: 8080
  externalTrafficPolicy: Local
  type: LoadBalancer