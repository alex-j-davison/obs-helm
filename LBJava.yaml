apiVersion: v1
kind: Service
metadata:
  name: Java-LB-service
spec:
  selector:
    app: k8-hello-java
  ports:
    - port: 8080
      targetPort: 80
  externalTrafficPolicy: Local
  type: LoadBalancer